services:
  gateway:
    build:
      context: ..
      dockerfile: docker/gateway.Dockerfile
    environment:
      PRISM_PROVIDER: mock
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

